apiVersion: v1
kind: Service
metadata:
  name: santo-alpha-svc
  labels:
    app: santo-alpha
spec:
  type: LoadBalancer
  selector:
    app: santo-alpha
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80